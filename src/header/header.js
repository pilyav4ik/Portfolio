import Menu from "./menu";

const Header = () => {
    return (
        <header>
            <div>
                Logo
            </div>
            <Menu/>
        </header>
    )
}

export default Header;